% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/post_proc_spend_plots.R
\name{jc_get_spend_line}
\alias{jc_get_spend_line}
\title{Gets a line graph showing a spending parameter over time.}
\usage{
jc_get_spend_line(
  spending_info,
  spend_param,
  x_info,
  y_info,
  cats_or_types,
  colours,
  line_types,
  line_width = 1,
  group_type = "category",
  legend_title = "Category",
  scale_factor = 1
)
}
\arguments{
\item{spending_info}{Spending info retrieved with
\code{link{jc_get_spending_info}}}

\item{spend_param}{spending parameter to plot - this should be one of the
columns in \code{spending_info} (e.g. 'total_cost', total_length',
'total_perc' or 'budget_util')}

\item{x_info}{List object containing attributes 'min', 'max', 'increm' and
'title' for X-axis (recommend leaving x-title blank).}

\item{y_info}{List object containing attributes 'min', 'max', 'increm' and
'title' for Y-axis.}

\item{cats_or_types}{Vector containing either the treatment Categories or the
treatment Types to include in the plot. If parameter \code{group_type} is
'category' then this list should be the treatment Categories you want to plot,
otherwise if parameter \code{group_type} is 'treatment' then this should be
the list of treatment Types you want to include in the plot.}

\item{colours}{Vector of colour names to match category names}

\item{line_types}{Vector of line style names to match category names
the statistics codes passed in as parameter \code{stat_codes} (valid
values are: 'solid', 'dashed', 'dotted', dotdash', 'longdash' etc. ).}

\item{line_width}{Line width for the lines. Try values in range 0.8 to 1.2.}

\item{group_type}{specifies whether grouping of treatments is by treatment
category or by treatment type (more specific). Pass either 'category' or
'treatment'}

\item{legend_title}{Title for the legend.}

\item{scale_factor}{scaling factor for values to be plotted (e.g. use 1e6 to
scale costs to $-million)}
}
\description{
\code{jc_get_spend_graph} Gets ggplot object showing lines representing
a spending parameter for different treatment categories over time.
}
\details{
Note: this function will plot the specified column values vs the 'period'
column. If you want the treatment period to be in terms of calendar-epochs,
then you should adjust the period column BEFORE calling this function.
}
